/**
 * Configuration pour les URLs API
 * Utilise /api pour passer par le proxy Vite (recommandé)
 * Ou utilise l'URL complète si le proxy ne fonctionne pas
 */

// Pour développement : utiliser le proxy Vite /api
// Pour production : utiliser l'URL complète de l'API
export const API_BASE_URL = '/api';

// Alternative : URL complète (en cas de besoin)
// export const API_BASE_URL = '/api';
